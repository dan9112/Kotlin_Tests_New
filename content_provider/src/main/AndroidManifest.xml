<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.KotlinTests">

        <provider
            android:name="com.example.content_provider.MyContactsProvider"
            android:authorities="ru.start_android.providers.AddressBook"
            android:enabled="true"
            android:exported="true"
            tools:ignore="ExportedContentProvider"/>

        <provider
            android:name="com.example.content_provider.CustomContentProvider"
            android:authorities="ru.lord.providers.Company"
            android:exported="true"
            android:grantUriPermissions="true"
            tools:ignore="ExportedContentProvider">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>
    </application>
</manifest>
